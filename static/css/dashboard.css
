/* ══════════════════════════════════════════════════════
   Rori Hotel — Dashboard Styles (Navy + Gold Luxury)
   ══════════════════════════════════════════════════════ */
:root{
  --rori-bg:#071426;
  --rori-bg-2:#0B1F3A;
  --rori-card:#0F2540;
  --rori-border:rgba(197,160,89,0.22);
  --rori-gold:#C5A059;
  --rori-gold-bright:#D4AF37;
  --rori-primary:#C5A059;
  --rori-primary-2:#B08D45;
  --rori-secondary:#3B82F6;
  --rori-accent:#EC4899;
  --rori-success:#10B981;
  --rori-warning:#F59E0B;
  --rori-danger:#EF4444;
  --rori-cyan:#06B6D4;
  --rori-text:#FFFFFF;
  --rori-text-2:#AAB4C3;
  --rori-text-3:#6B7280;
  --rori-sidebar-w:260px;
}
*{box-sizing:border-box}
html,body{margin:0;padding:0}
body.rori-dashboard{
  font-family:'Inter',system-ui,-apple-system,sans-serif;
  background:
    radial-gradient(circle at 15% 0%,rgba(197,160,89,0.10) 0%,transparent 45%),
    radial-gradient(circle at 85% 100%,rgba(11,31,58,0.55) 0%,transparent 45%),
    var(--rori-bg);
  color:var(--rori-text);
  min-height:100vh;
  -webkit-font-smoothing:antialiased;
}
a{color:inherit}
.rori-layout{display:flex;min-height:100vh}

.rori-sidebar{
  width:var(--rori-sidebar-w);
  background:linear-gradient(180deg,#0B1F3A 0%,#071426 100%);
  border-right:1px solid var(--rori-border);
  padding:1.5rem 0;
  position:fixed;top:0;left:0;bottom:0;
  overflow-y:auto;z-index:100;
}
.rori-sidebar::-webkit-scrollbar{width:6px}
.rori-sidebar::-webkit-scrollbar-thumb{background:rgba(197,160,89,0.4);border-radius:6px}

.rori-sidebar-brand{
  display:flex;align-items:center;gap:.75rem;
  padding:0 1.5rem 1.5rem;
  border-bottom:1px solid var(--rori-border);
  margin-bottom:1rem;
}
.rori-sidebar-brand .logo{
  width:42px;height:42px;border-radius:12px;
  background:linear-gradient(135deg,var(--rori-gold),var(--rori-gold-bright));
  display:flex;align-items:center;justify-content:center;
  font-size:1.15rem;color:#071426;
  box-shadow:0 0 20px rgba(197,160,89,0.5);
  flex-shrink:0;
}
.rori-sidebar-brand h2{
  font-size:1.05rem;margin:0;font-weight:800;
  background:linear-gradient(135deg,#fff,var(--rori-gold));
  -webkit-background-clip:text;background-clip:text;
  color:transparent;letter-spacing:.3px;
}
.rori-sidebar-brand p{margin:.15rem 0 0;font-size:.68rem;color:var(--rori-text-2);letter-spacing:.5px;text-transform:uppercase}

.rori-nav{display:flex;flex-direction:column;padding:0 .75rem;gap:.15rem}
.rori-nav a{
  display:flex;align-items:center;gap:.75rem;
  padding:.7rem 1rem;
  color:var(--rori-text-2);
  text-decoration:none;
  border-radius:12px;
  font-size:.88rem;
  font-weight:500;
  transition:all .18s ease;
  border:1px solid transparent;
}
.rori-nav a i{font-size:.95rem;width:18px;text-align:center;color:inherit}
.rori-nav a:hover{
  background:rgba(197,160,89,0.10);
  color:#fff;
  border-color:rgba(197,160,89,0.30);
  transform:translateX(2px);
}
.rori-nav a.active{
  background:linear-gradient(135deg,rgba(197,160,89,0.22),rgba(11,31,58,0.30));
  color:#fff;
  border-color:rgba(197,160,89,0.50);
  box-shadow:0 0 20px rgba(197,160,89,0.18);
}
.rori-nav a.active i{color:var(--rori-gold)}

.rori-main{
  flex:1;margin-left:var(--rori-sidebar-w);
  padding:1.5rem 2rem 3rem;
  max-width:calc(100vw - var(--rori-sidebar-w));
  min-width:0;
}
.rori-header{
  display:flex;justify-content:space-between;align-items:center;
  margin-bottom:1.5rem;flex-wrap:wrap;gap:1rem;
}
.rori-header h1{
  margin:0;font-size:1.55rem;font-weight:800;line-height:1.2;
  background:linear-gradient(135deg,#fff 0%,var(--rori-gold) 100%);
  -webkit-background-clip:text;background-clip:text;color:transparent;
}
.rori-header .subtitle{color:var(--rori-text-2);font-size:.82rem;margin-top:.25rem}

.rori-user{
  display:flex;align-items:center;gap:.75rem;
  padding:.5rem 1rem;
  background:var(--rori-card);
  border:1px solid var(--rori-border);
  border-radius:40px;
}
.rori-user .avatar{
  width:34px;height:34px;border-radius:50%;
  background:linear-gradient(135deg,var(--rori-gold),var(--rori-gold-bright));
  display:flex;align-items:center;justify-content:center;
  font-size:.85rem;font-weight:700;color:#071426;
}
.rori-user .info{display:flex;flex-direction:column;line-height:1.2}
.rori-user .info strong{font-size:.85rem;color:#fff}
.rori-user .info small{font-size:.68rem;color:var(--rori-text-2)}

.rori-filters{
  background:var(--rori-card);
  border:1px solid var(--rori-border);
  border-radius:16px;
  padding:1rem 1.25rem;
  margin-bottom:1.5rem;
  box-shadow:0 4px 20px rgba(0,0,0,0.3);
}
.rori-filters-form{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(145px,1fr));
  gap:.75rem;align-items:end;
}
.rori-filters label{
  display:block;font-size:.68rem;color:var(--rori-text-2);
  text-transform:uppercase;letter-spacing:.5px;margin-bottom:.35rem;font-weight:600;
}
.rori-filters select,
.rori-filters input{
  width:100%;
  background:var(--rori-bg);
  border:1px solid rgba(197,160,89,0.25);
  color:#fff;
  padding:.55rem .75rem;
  border-radius:10px;
  font-size:.82rem;outline:none;
  transition:border .2s,box-shadow .2s;
  font-family:inherit;
}
.rori-filters select:focus,
.rori-filters input:focus{
  border-color:var(--rori-gold);
  box-shadow:0 0 0 3px rgba(197,160,89,0.15);
}
.rori-filters select option{background:#0B1F3A;color:#fff}
.rori-filters-buttons{display:flex;gap:.5rem;flex-wrap:wrap}

.rori-btn{
  padding:.6rem 1.15rem;
  border-radius:10px;border:none;
  font-size:.82rem;font-weight:600;
  cursor:pointer;text-decoration:none;
  display:inline-flex;align-items:center;gap:.4rem;
  transition:all .2s;white-space:nowrap;
  font-family:inherit;
}
.rori-btn-primary{
  background:linear-gradient(135deg,var(--rori-gold),var(--rori-primary-2));
  color:#071426;
  box-shadow:0 4px 15px rgba(197,160,89,0.35);
}
.rori-btn-primary:hover{box-shadow:0 6px 22px rgba(197,160,89,0.55);color:#071426;transform:translateY(-1px)}
.rori-btn-ghost{
  background:transparent;color:var(--rori-text-2);
  border:1px solid var(--rori-border);
}
.rori-btn-ghost:hover{color:#fff;border-color:var(--rori-gold);background:rgba(197,160,89,0.08)}

.rori-kpis{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(220px,1fr));
  gap:1rem;margin-bottom:1.5rem;
}
.rori-kpi{
  background:var(--rori-card);
  border:1px solid var(--rori-border);
  border-radius:16px;
  padding:1.25rem;
  position:relative;overflow:hidden;
  transition:transform .2s,box-shadow .2s;
  min-height:130px;
}
.rori-kpi::before{
  content:'';position:absolute;top:0;left:0;right:0;height:3px;
  background:linear-gradient(90deg,var(--rori-gold),var(--rori-gold-bright));
  opacity:.9;
}
.rori-kpi:hover{transform:translateY(-3px);box-shadow:0 12px 30px rgba(197,160,89,0.18)}
.rori-kpi.purple::before{background:linear-gradient(90deg,var(--rori-gold),var(--rori-accent))}
.rori-kpi.blue::before{background:linear-gradient(90deg,var(--rori-secondary),var(--rori-cyan))}
.rori-kpi.pink::before{background:linear-gradient(90deg,var(--rori-accent),var(--rori-gold))}
.rori-kpi.green::before{background:linear-gradient(90deg,var(--rori-success),var(--rori-cyan))}

.rori-kpi-label{
  font-size:.7rem;color:var(--rori-text-2);
  text-transform:uppercase;letter-spacing:1px;
  font-weight:600;margin-bottom:.6rem;
}
.rori-kpi-value{
  font-size:2rem;font-weight:800;color:#fff;
  line-height:1;margin-bottom:.65rem;
  letter-spacing:-.02em;
}
.rori-kpi-sub{
  font-size:.75rem;color:var(--rori-text-2);
  display:flex;align-items:center;gap:.35rem;flex-wrap:wrap;
}
.rori-kpi-sub.up{color:var(--rori-success)}
.rori-kpi-sub.down{color:var(--rori-danger)}
.rori-kpi-sub.neutral{color:var(--rori-text-2)}

.rori-kpi-icon{
  position:absolute;top:1rem;right:1rem;
  width:40px;height:40px;border-radius:12px;
  display:flex;align-items:center;justify-content:center;
  font-size:1.05rem;color:var(--rori-gold);
  background:rgba(197,160,89,0.12);
}
.rori-kpi.blue .rori-kpi-icon{background:rgba(59,130,246,0.12);color:var(--rori-secondary)}
.rori-kpi.pink .rori-kpi-icon{background:rgba(236,72,153,0.12);color:var(--rori-accent)}
.rori-kpi.green .rori-kpi-icon{background:rgba(16,185,129,0.12);color:var(--rori-success)}

.rori-card{
  background:var(--rori-card);
  border:1px solid var(--rori-border);
  border-radius:18px;
  padding:1.25rem 1.5rem;
  box-shadow:0 4px 20px rgba(0,0,0,0.3);
}
.rori-card-header{
  display:flex;justify-content:space-between;align-items:center;
  margin-bottom:1rem;flex-wrap:wrap;gap:.5rem;
}
.rori-card-header h3{
  margin:0;font-size:1.02rem;font-weight:600;color:#fff;
}
.rori-card-header h3 i{color:var(--rori-gold);margin-right:.5rem}
.rori-card-header .hint{
  font-size:.7rem;color:var(--rori-text-2);
  background:rgba(197,160,89,0.1);
  padding:.2rem .65rem;border-radius:20px;
  text-transform:uppercase;letter-spacing:.5px;
  font-weight:600;
}
.rori-grid-2{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(320px,1fr));
  gap:1rem;margin-bottom:1.5rem;
}
.rori-section{margin-bottom:1.5rem}

.rori-chart-wrap{position:relative;height:300px;width:100%}
.rori-chart-wrap.tall{height:340px}
.rori-chart-wrap.donut{height:280px}

.rori-table-wrap{overflow-x:auto;-webkit-overflow-scrolling:touch}
.rori-table{width:100%;border-collapse:collapse;font-size:.82rem}
.rori-table th{
  text-align:left;padding:.75rem;
  color:var(--rori-gold);
  font-size:.68rem;text-transform:uppercase;letter-spacing:.5px;
  border-bottom:1px solid var(--rori-border);
  font-weight:600;white-space:nowrap;
}
.rori-table td{
  padding:.75rem;
  border-bottom:1px solid rgba(197,160,89,0.08);
  color:#e5e7eb;vertical-align:middle;
}
.rori-table tr:last-child td{border-bottom:none}
.rori-table tbody tr:hover{background:rgba(197,160,89,0.05)}
.rori-table a{color:var(--rori-gold);text-decoration:none;font-weight:600}
.rori-table a:hover{text-decoration:underline}

.rori-badge{
  display:inline-block;padding:.25rem .65rem;
  border-radius:20px;font-size:.68rem;font-weight:700;
  text-transform:uppercase;letter-spacing:.3px;
  white-space:nowrap;
}
.rori-badge.b-pending{background:rgba(245,158,11,0.15);color:var(--rori-warning);border:1px solid rgba(245,158,11,0.3)}
.rori-badge.b-approved{background:rgba(59,130,246,0.15);color:var(--rori-secondary);border:1px solid rgba(59,130,246,0.3)}
.rori-badge.b-assigned{background:rgba(197,160,89,0.15);color:var(--rori-gold);border:1px solid rgba(197,160,89,0.3)}
.rori-badge.b-inprogress{background:rgba(59,130,246,0.15);color:var(--rori-secondary);border:1px solid rgba(59,130,246,0.3)}
.rori-badge.b-completed{background:rgba(16,185,129,0.15);color:var(--rori-success);border:1px solid rgba(16,185,129,0.3)}
.rori-badge.b-verified{background:rgba(6,182,212,0.15);color:var(--rori-cyan);border:1px solid rgba(6,182,212,0.3)}
.rori-badge.b-closed{background:rgba(16,185,129,0.2);color:#34d399;border:1px solid rgba(16,185,129,0.4)}
.rori-badge.b-rejected{background:rgba(239,68,68,0.15);color:var(--rori-danger);border:1px solid rgba(239,68,68,0.3)}
.rori-badge.b-overdue{background:rgba(239,68,68,0.2);color:#f87171;border:1px solid rgba(239,68,68,0.4)}
.rori-badge.b-default{background:rgba(156,163,175,0.15);color:var(--rori-text-2);border:1px solid rgba(156,163,175,0.3)}
.rori-badge.b-urgent{background:rgba(239,68,68,0.15);color:var(--rori-danger);border:1px solid rgba(239,68,68,0.3)}
.rori-badge.b-high{background:rgba(245,158,11,0.15);color:var(--rori-warning);border:1px solid rgba(245,158,11,0.3)}
.rori-badge.b-medium{background:rgba(59,130,246,0.15);color:var(--rori-secondary);border:1px solid rgba(59,130,246,0.3)}
.rori-badge.b-low{background:rgba(16,185,129,0.15);color:var(--rori-success);border:1px solid rgba(16,185,129,0.3)}

.rori-stats-grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(105px,1fr));
  gap:.75rem;
}
.rori-stat{
  padding:.85rem .6rem;
  background:var(--rori-bg);
  border:1px solid var(--rori-border);
  border-radius:12px;
  text-align:center;text-decoration:none;
  transition:all .2s;display:block;
}
.rori-stat:hover{
  border-color:var(--rori-gold);
  box-shadow:0 0 15px rgba(197,160,89,0.22);
  transform:translateY(-2px);
}
.rori-stat .val{font-size:1.4rem;font-weight:800;color:#fff;line-height:1}
.rori-stat .lbl{
  font-size:.65rem;color:var(--rori-text-2);
  text-transform:uppercase;letter-spacing:.3px;
  margin-top:.35rem;font-weight:600;
}

.rori-list{display:flex;flex-direction:column;gap:.5rem}
.rori-list-item{
  display:flex;align-items:center;gap:.75rem;
  padding:.65rem .85rem;
  background:var(--rori-bg);
  border:1px solid var(--rori-border);
  border-radius:10px;font-size:.85rem;
  transition:all .15s;
}
.rori-list-item:hover{border-color:rgba(197,160,89,0.5);background:rgba(197,160,89,0.05)}
.rori-list-item .rank{
  width:26px;height:26px;border-radius:8px;
  display:flex;align-items:center;justify-content:center;
  background:rgba(197,160,89,0.15);color:var(--rori-gold);
  font-weight:700;font-size:.72rem;flex-shrink:0;
}
.rori-list-item .name{flex:1;color:#e5e7eb}
.rori-list-item .count{font-weight:800;color:var(--rori-gold);font-size:.95rem}

.rori-activity{display:flex;flex-direction:column;gap:.6rem}
.rori-activity-item{
  display:flex;gap:.75rem;padding:.7rem .85rem;
  background:var(--rori-bg);
  border-left:3px solid var(--rori-gold);
  border-radius:8px;font-size:.85rem;
}
.rori-activity-item .icon{
  width:32px;height:32px;border-radius:8px;
  background:rgba(197,160,89,0.15);
  display:flex;align-items:center;justify-content:center;
  color:var(--rori-gold);font-size:.8rem;flex-shrink:0;
}
.rori-activity-item .txt{flex:1;color:#e5e7eb;min-width:0}
.rori-activity-item .txt strong{color:#fff}
.rori-activity-item .time{font-size:.68rem;color:var(--rori-text-3);margin-top:.2rem}

.rori-empty{
  text-align:center;padding:2rem 1rem;
  color:var(--rori-text-2);font-size:.85rem;
}
.rori-empty i{
  font-size:2rem;color:var(--rori-gold);
  opacity:.5;display:block;margin-bottom:.5rem;
}

.rori-mobile-toggle{
  display:none;width:40px;height:40px;
  border-radius:10px;
  background:var(--rori-card);
  border:1px solid var(--rori-border);
  color:#fff;align-items:center;justify-content:center;
  cursor:pointer;font-size:1rem;
}
.rori-backdrop{
  display:none;position:fixed;inset:0;
  background:rgba(0,0,0,0.55);z-index:99;
  backdrop-filter:blur(2px);
}
.rori-backdrop.show{display:block}

@media (max-width:1100px){
  .rori-main{padding:1.25rem}
}
@media (max-width:992px){
  .rori-sidebar{
    transform:translateX(-100%);
    transition:transform .3s ease;
    box-shadow:4px 0 30px rgba(0,0,0,0.6);
  }
  .rori-sidebar.open{transform:translateX(0)}
  .rori-main{margin-left:0;max-width:100vw;padding:1rem}
  .rori-mobile-toggle{display:flex}
  .rori-header h1{font-size:1.25rem}
  .rori-kpi-value{font-size:1.6rem}
  .rori-chart-wrap{height:260px}
  .rori-chart-wrap.tall{height:280px}
  .rori-chart-wrap.donut{height:240px}
}
@media (max-width:576px){
  .rori-user .info{display:none}
  .rori-user{padding:.4rem}
  .rori-kpis{grid-template-columns:1fr 1fr}
  .rori-kpi{padding:1rem;min-height:110px}
  .rori-kpi-value{font-size:1.35rem}
  .rori-card{padding:1rem}
  .rori-filters-form{grid-template-columns:1fr 1fr}
  .rori-filters-buttons{grid-column:1/-1}
}